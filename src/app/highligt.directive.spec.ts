import { HighligtDirective } from './highligt.directive';

describe('HighligtDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligtDirective();
    expect(directive).toBeTruthy();
  });
});
